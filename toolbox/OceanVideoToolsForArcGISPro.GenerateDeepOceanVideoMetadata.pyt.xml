<?xml version="1.0" encoding="UTF-8"?>
<metadata xml:lang="en">
    <Esri>
        <CreaDate>20260828</CreaDate>
        <CreaTime>15060200</CreaTime>
        <ArcGISFormat>1.0</ArcGISFormat>
        <SyncOnce>TRUE</SyncOnce>
    </Esri>
    <tool name="GenerateDeepOceanVideoMetadata" displayname="Generate Deep Ocean Video Metadata" toolboxalias="OceanVideoTools" xmlns="">
        <parameters>
            <param name="telemetry_table" displayname="Input Navigation / Telemetry Table (raw vehicle log)" type="Required" direction="Input" datatype="Table View" expression="telemetry_table">
                <dialogReference>
                    <para>The vehicle's navigation/telemetry log - a CSV, Excel worksheet, or geodatabase table with one row per time step. Must contain an X/longitude column, a Y/latitude column, and a timestamp column; rows missing any of those three are skipped. Everything else the multiplexer needs that the log does not have (camera angles, field of view, near/far distance, camera height) is supplied by the Video Acquisition Profile below.</para>
                </dialogReference>
            </param>
            <param name="video_acquisition_profile" displayname="Video Acquisition Profile" type="Required" direction="Input" datatype="String" expression="video_acquisition_profile">
                <dialogReference>
                    <para>Camera mounting and pointing configuration. Supplies estimates for the parameters a navigation log does not normally record: camera pitch and roll relative to the vehicle, horizontal and vertical field of view, near and far distance, and height above the seafloor. Selecting a profile pre-fills the override fields below with its values. Edits to those fields are retained until the profile selection changes, which overwrites them. The Custom profile pre-fills nothing and overwrites nothing; supply every value in the override fields.</para>
                </dialogReference>
            </param>
            <param name="output_folder" displayname="Output Folder for Video Metadata File" type="Required" direction="Input" datatype="Folder" expression="output_folder">
                <dialogReference>
                    <para>Folder the output CSV files are written into. A folder is used rather than a geodatabase because the downstream Esri tools (Convert Video Metadata, Video Multiplexer) only accept CSV, JSON or GPX files.</para>
                </dialogReference>
            </param>
            <param name="output_name" displayname="Output Table Name" type="Required" direction="Input" datatype="String" expression="output_name">
                <dialogReference>
                    <para>Base name for the outputs, without an extension. Three files are produced: the final multiplexer-ready metadata CSV, this tool's own pre-conversion table, and the field mapping file. Existing files of the same name are overwritten.</para>
                </dialogReference>
            </param>
            <param name="create_track_fc" displayname="Create Sensor Track Point Feature Class (QA layer)" type="Required" direction="Input" datatype="Boolean" expression="create_track_fc">
                <dialogReference>
                    <para>Writes a point feature class of the resolved track, one point per output row. Quality control only; no downstream tool consumes it. Displaying it on a map confirms the reprojected positions before multiplexing, and is the practical check on the Coordinate System of Telemetry X/Y setting.</para>
                </dialogReference>
            </param>
            <param name="output_gdb" displayname="Output Geodatabase for Sensor Track Feature Class" type="Optional" direction="Input" datatype="Workspace" expression="{output_gdb}">
                <dialogReference>
                    <para>Geodatabase the QA track feature class is written into. Required only when Create Sensor Track Point Feature Class is checked.</para>
                </dialogReference>
            </param>
            <param name="input_srs" displayname="Coordinate System of Telemetry X/Y" type="Optional" direction="Input" datatype="Coordinate System" expression="{input_srs}">
                <dialogReference>
                    <para>The coordinate system the telemetry X/Y values are already in, not a system to convert them to. X/Y are reprojected to WGS84 longitude/latitude, which MISB ST 0601 requires. Leave at WGS84 only if the log holds degrees; set the projected system if it holds eastings and northings. A coordinate system that is valid but incorrect for the data produces no error, only relocated coordinates.</para>
                </dialogReference>
            </param>
            <param name="x_field" displayname="X / Longitude / Easting Field" type="Optional" direction="Input" datatype="String" expression="{x_field}">
                <dialogReference>
                    <para>Column holding the east-west position: longitude if the log is geographic, easting if it is projected. Leave blank to auto-detect by name. Required: rows with no usable X are skipped.</para>
                    <para>Units follow the Coordinate System of Telemetry X/Y above - decimal degrees for a geographic system, linear units (usually metres) for a projected one. Values are read as written. Degrees-minutes-seconds text is not parsed and must be converted to decimal degrees first. A value with six or seven digits before the decimal point is an easting, not a longitude.</para>
                    <para>The column is reprojected from the declared coordinate system to WGS84 longitude/latitude. A wrong declared system produces no error, only relocated coordinates; verify the output positions before multiplexing.</para>
                </dialogReference>
            </param>
            <param name="y_field" displayname="Y / Latitude / Northing Field" type="Optional" direction="Input" datatype="String" expression="{y_field}">
                <dialogReference>
                    <para>Column holding the north-south position: latitude if the log is geographic, northing if it is projected. Leave blank to auto-detect by name. Required: rows with no usable Y are skipped.</para>
                    <para>Units follow the Coordinate System of Telemetry X/Y above - decimal degrees for a geographic system, linear units (usually metres) for a projected one. Values are read as written. Degrees-minutes-seconds text is not parsed and must be converted to decimal degrees first. A northing in the millions from a southern-hemisphere survey includes a 10,000,000 m false northing and requires a southern UTM zone.</para>
                    <para>The column is reprojected from the declared coordinate system to WGS84 longitude/latitude. A wrong declared system produces no error, only relocated coordinates; verify the output positions before multiplexing.</para>
                </dialogReference>
            </param>
            <param name="time_field" displayname="Timestamp Field (for multiplexing order)" type="Optional" direction="Input" datatype="String" expression="{time_field}">
                <dialogReference>
                    <para>Column holding each row's date and time. Leave blank to auto-detect by name. Required: rows with a missing or unparseable timestamp are skipped, because the multiplexer aligns metadata to video frames by time. If no row parses, the run fails with 'No telemetry rows had usable X, Y, and Timestamp values'.</para>
                    <para>Accepted forms: a Date-type field; an epoch number in seconds, milliseconds or microseconds; ISO 8601 text (2022-09-01T00:00:00Z); M/D/YYYY with or without AM/PM; YYYY/MM/DD; M-D-YYYY; and dot-separated day-first D.M.YYYY, in which 01.09.2022 is 1 September. Unix time is accepted but not required.</para>
                    <para>The value is converted, not passed through. Each parsed time is written as Precision Time Stamp (integer microseconds since 1 January 1970, as MISB and the multiplexer require) and as an AcquisitionDate copy. Rows are sorted into time order before further processing.</para>
                    <para>A timestamp with no time zone is treated as UTC. A log recorded in local time multiplexes correctly, since row order and spacing are unaffected, but its absolute times are offset by that zone. Convert to UTC first if the video must align with other time-stamped records.</para>
                </dialogReference>
            </param>
            <param name="z_field" displayname="Z / Depth / Altitude Field" type="Optional" direction="Input" datatype="String" expression="{z_field}">
                <dialogReference>
                    <para>Column holding the platform's vertical position, written as-is to Sensor True Altitude. Leave blank to auto-detect by name. Optional: rows with no Z are retained. If the column is absent and no constant is set below, Sensor True Altitude is empty.</para>
                    <para>Sensor True Altitude is metres above mean sea level, positive up. No unit or sign conversion is applied. A depth column, which records positive-down values in most vehicle logs, must be negated before use: an unmodified 921.62 states a position 921 m above sea level rather than 921 m below it. Feet are not converted to metres.</para>
                    <para>This is the platform's position relative to sea level, not its height above the seafloor. An altimeter reading belongs in Camera Height Above Seafloor under Video Acquisition Profile Overrides. Values are not validated against bathymetry.</para>
                </dialogReference>
            </param>
            <param name="z_constant" displayname="Constant Z Value (used where the telemetry has none)" type="Optional" direction="Input" datatype="Double" expression="{z_constant}">
                <dialogReference>
                    <para>Applied to every row whose Z is missing, and to all rows when the table has no Z column. Rows carrying their own Z are unchanged. Use it where the platform held a known working depth or altitude that the log did not record. Units and sign follow the Z field above: metres above mean sea level, positive up, so a working depth of 900 m is entered as -900. No conversion is applied.</para>
                </dialogReference>
            </param>
            <param name="z_is_ellipsoid_height" displayname="Telemetry Z is a height above the ellipsoid" type="Optional" direction="Input" datatype="Boolean" expression="{z_is_ellipsoid_height}">
                <dialogReference>
                    <para>Writes Z to Sensor Ellipsoid Height Extended in addition to Sensor True Altitude. Ellipsoid height and height above mean sea level are different references, and no bathymetry or geoid model is available here to convert between them, so enable this only for a log that records ellipsoid height. When off, the field is empty and the multiplexer reports it as a missing value. An explicit Additional Field Mapping to that field takes precedence over this setting.</para>
                </dialogReference>
            </param>
            <param name="write_far_distance" displayname="Write Sensor Far Distance to the multiplexer file" type="Optional" direction="Input" datatype="Boolean" expression="{write_far_distance}">
                <dialogReference>
                    <para>Off by default. Sensor Far Distance is not required to draw a video footprint, and a value shorter than the camera-to-ground range clips the footprint to nothing: a profile's few metres cannot reach a seafloor tens of metres away. Enable it only where the profile's far distance exceeds that range. The value is written to the intermediate table in either case, where the imagery tools read it.</para>
                </dialogReference>
            </param>
            <param name="additional_field_mapping" displayname="Additional Field Mapping" type="Optional" direction="Input" datatype="Value Table" expression="{additional_field_mapping}">
                <dialogReference>
                    <para>Maps an additional telemetry column to a video metadata field, for example a recorded heading, pitch, roll or field of view in place of the profile's assumed value. Enter the column name on the left exactly as it appears in the table header - the Z / Depth / Altitude Field drop-down above lists the same names - and select its destination on the right. A mapped value overrides the profile or auto-detected value; a blank cell leaves the existing value unchanged. ObjectID, Precision Time Stamp, AcquisitionDate, Sensor Longitude and Sensor Latitude are not listed because this tool computes them. An unrecognised column name is reported as a warning and skipped rather than failing the run.</para>
                </dialogReference>
            </param>
            <param name="camera_pitch_override" displayname="Camera Tilt from Nadir (degrees, 0 = straight down, 90 = horizon)" type="Optional" direction="Input" datatype="Double" expression="{camera_pitch_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="camera_roll_override" displayname="Sensor Relative Roll Angle (degrees)" type="Optional" direction="Input" datatype="Double" expression="{camera_roll_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="heading_constant_override" displayname="Constant Platform Heading (degrees, used if no track/field)" type="Optional" direction="Input" datatype="Double" expression="{heading_constant_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="hfov_override" displayname="Sensor Horizontal Field of View (degrees)" type="Optional" direction="Input" datatype="Double" expression="{hfov_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="vfov_override" displayname="Sensor Vertical Field of View (degrees)" type="Optional" direction="Input" datatype="Double" expression="{vfov_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="camera_height_override" displayname="Camera Height Above Seafloor (meters)" type="Optional" direction="Input" datatype="Double" expression="{camera_height_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="near_distance_override" displayname="Near Distance (meters)" type="Optional" direction="Input" datatype="Double" expression="{near_distance_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="far_distance_override" displayname="Far Distance (meters)" type="Optional" direction="Input" datatype="Double" expression="{far_distance_override}">
                <dialogReference>
                    <para>Pre-filled from the Video Acquisition Profile selected above; edit it to match the camera rig in use. The value is written to every output row. Edits are retained until the profile selection changes, which overwrites this field. The Custom profile pre-fills nothing and overwrites nothing.</para>
                </dialogReference>
            </param>
            <param name="import_profile_template" displayname="Import Video Acquisition Profile Template (optional)" type="Optional" direction="Input" datatype="File" expression="{import_profile_template}">
                <dialogReference>
                    <para>A JSON file of camera settings saved by a previous run's Export Resolved Profile option. Applies one vehicle's measured rig settings across multiple dives of a cruise. Values loaded here fill the override fields above.</para>
                </dialogReference>
            </param>
            <param name="export_profile_template" displayname="Export Resolved Profile as Template for Future Runs" type="Optional" direction="Input" datatype="Boolean" expression="{export_profile_template}">
                <dialogReference>
                    <para>Saves the camera settings used by this run - the selected profile plus any edits to the override fields - as a JSON file for import into later runs of the same vehicle.</para>
                </dialogReference>
            </param>
            <param name="profile_template_output" displayname="Profile Template Output File (.json)" type="Optional" direction="Output" datatype="File" expression="{profile_template_output}">
                <dialogReference>
                    <para>Where to save the profile template JSON. Enabled only when Export Resolved Profile as Template is checked.</para>
                </dialogReference>
            </param>
            <param name="resample_interval" displayname="Resample Telemetry to Fixed Time Interval (seconds, optional)" type="Optional" direction="Input" datatype="Double" expression="{resample_interval}">
                <dialogReference>
                    <para>Thins the telemetry to one row per this many seconds. Leave blank to keep every row. A high-rate log - 10 Hz over a long dive, for example - produces a very large metadata file; one row per second is generally sufficient for multiplexing.</para>
                </dialogReference>
            </param>
            <param name="camera_id" displayname="Camera ID" type="Required" direction="Input" datatype="Long" expression="camera_id">
                <dialogReference>
                    <para>Identifier for the camera these settings describe. One physical camera per run, so the default of 1 applies unless several cameras' outputs are kept distinct.</para>
                </dialogReference>
            </param>
            <param name="camera_ncols" displayname="Camera Sensor Columns (pixels)" type="Optional" direction="Input" datatype="Long" expression="{camera_ncols}">
                <dialogReference>
                    <para>Video frame width in pixels, e.g. 1920 for HD. Optional.</para>
                </dialogReference>
            </param>
            <param name="camera_nrows" displayname="Camera Sensor Rows (pixels)" type="Optional" direction="Input" datatype="Long" expression="{camera_nrows}">
                <dialogReference>
                    <para>Video frame height in pixels, e.g. 1080 for HD. Optional.</para>
                </dialogReference>
            </param>
            <param name="camera_focal_length" displayname="Camera Focal Length" type="Optional" direction="Input" datatype="Double" expression="{camera_focal_length}">
                <dialogReference>
                    <para>Lens focal length, in the same units as Camera Pixel Size (Esri's Cameras table schema documents both in microns). Optional - leave blank if unknown.</para>
                </dialogReference>
            </param>
            <param name="camera_pixel_size" displayname="Camera Pixel Size" type="Optional" direction="Input" datatype="Double" expression="{camera_pixel_size}">
                <dialogReference>
                    <para>Physical size of one sensor pixel, in the same units as Camera Focal Length. Optional - leave blank if unknown.</para>
                </dialogReference>
            </param>
            <param name="out_video_metadata_table" displayname="Output Video Metadata Table (Multiplexer-ready)" type="Derived" direction="Output" datatype="File" expression="{out_video_metadata_table}" />
            <param name="out_metadata_mapping_file" displayname="Output Metadata Mapping File" type="Derived" direction="Output" datatype="File" expression="{out_metadata_mapping_file}" />
            <param name="out_track_fc" displayname="Output Sensor Track Feature Class" type="Derived" direction="Output" datatype="Feature Class" expression="{out_track_fc}" />
        </parameters>
        <summary>
            <para>Builds a metadata CSV for Esri's Video Multiplexer from a vehicle's navigation/telemetry log.</para>
            <para>X, Y and a timestamp are the only required columns. Camera parameters that a navigation log does not normally record - camera pitch and roll, heading, field of view, near and far distance, height above the seafloor - are supplied by a selectable Video Acquisition Profile (remotely operated vehicle, autonomous underwater vehicle, towed sled, drop camera) and can be edited before the run. Heading can also be computed from the track. Positions are reprojected to WGS84 longitude/latitude, as MISB ST 0601 requires.</para>
            <para>Requires the Image Analyst extension: the final field-naming and formatting pass is delegated to Convert Video Metadata rather than reimplemented here.</para>
        </summary>
    </tool>
    <dataIdInfo>
        <idCitation>
            <resTitle>Generate Deep Ocean Video Metadata</resTitle>
        </idCitation>
    </dataIdInfo>
</metadata>
